/*#include <stdio.h>
#include <stdbool.h>

int main()
{
    bool isOnline = true;
    printf("%d", isOnline);

    return 0;
} */


#include <stdio.h>
#include <stdbool.h>

int main()
{
    bool isOnline = 0;
    if(isOnline){
        printf("User is online.");
    }
    else{
        printf("User is offline.");
    }

    return 0;
}
